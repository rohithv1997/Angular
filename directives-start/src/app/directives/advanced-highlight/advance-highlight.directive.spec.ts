import { AdvanceHighlightDirective } from './advance-highlight.directive';

describe('AdvanceHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
